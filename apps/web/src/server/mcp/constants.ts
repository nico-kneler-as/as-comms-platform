export const MCP_SERVER_NAME = "as-comms-mcp"
// Reported as `serverInfo.version` in the MCP handshake, so keep it semver.
export const MCP_CONNECTOR_VERSION = "0.1.0"
export const DEFAULT_MCP_ENVIRONMENT_NAME = "development"
