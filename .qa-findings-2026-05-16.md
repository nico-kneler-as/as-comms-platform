# Stage 5A Phase A QA Findings — 2026-05-16

Scope: campaigns list, new-campaign wizard, run detail, public unsubscribe, sender verification, Postmark webhook, and 30-day finalization cron.

## Environment Notes

- Dev server could not bind inside this sandbox. `pnpm --filter @as-comms/web dev` failed with `listen EPERM: operation not permitted 0.0.0.0:3000`; `next dev -H 127.0.0.1 -p 3000` also failed with `listen EPERM`. Browser QA is blocked here.
- Static inspection and unit tests were used for this pass. Existing unit suite passed before edits.
- `git fetch origin main` failed because `.git/FETCH_HEAD` could not be opened in the sandbox. Branch was created from the existing local `origin/main` ref.

## Findings

| Severity | Surface                               | Finding                                                                                                                                                                                     | Status            |
| -------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| P0       | `/campaigns/new`                      | New campaign draft creation validates `kind='project'` with `projectId=null`, but a fresh draft has no audience/project yet. This makes the wizard inaccessible before Step 3.              | Fixed             |
| P0       | `/campaigns/[runId]`                  | Mailchimp rows link to run detail with `provider=mailchimp`, but the detail loader only reads native `campaign_runs`, so historical rows 404.                                               | Fixed             |
| P1       | `/campaigns`                          | Campaigns list was visually inconsistent with the locked prototype and Inbox row rhythm: oversized row cards, boxed filter panel, narrow content width, and missing icon tile row shape.    | Fixed             |
| P1       | `/campaigns/[runId]`                  | Duplicate action was visible for every state and would be offered for draft/scheduled/sending runs, contrary to the spec.                                                                   | Fixed             |
| P1       | Postmark webhook                      | Missing signatures returned 401; malformed JSON returned 200 after valid signature. QA contract requires missing-signature 400, mismatch 401, malformed JSON 400, unknown events 200 no-op. | Fixed             |
| P2       | Campaign timestamps                   | Run detail timestamps used the browser/default timezone and list rows used UTC display. Locked timezone is America/Denver.                                                                  | Fixed             |
| P2       | `/campaigns/[runId]`                  | Historical Mailchimp detail has no per-recipient imported rows; recipient/audit empty states render, but detail metrics are necessarily limited to imported projection data.                | Documented        |
| P2       | `/campaigns`                          | Pagination is not implemented; list reads up to 200 rows and virtualizes client-side. Spec asks to note this if absent.                                                                     | Documented        |
| P2       | `/campaigns/new` Step 5               | Review sender dropdown currently includes verified senders only. The spec asks to show unverified aliases with a tooltip. This likely needs a broader sender option view-model change.      | Open              |
| P2       | `/campaigns/new` Step 2               | Newsletter role gating is not visible in the UI code path; current flat roles are `admin` and `operator`.                                                                                   | Open              |
| P2       | `/campaigns/new` browser back/forward | Wizard state is draft-backed only on saves/autosave; navigating away before a save can lose unsaved local edits without a warning.                                                          | Open              |
| P3       | `/u/[token]`                          | Public unsubscribe unit coverage already verifies valid, invalid, idempotent, all-scope redirect, snapshots, and footer address rendering.                                                  | Verified by tests |
| P3       | Worker finalization                   | 30-day finalization cron selects `complete` runs older than 30 days, transitions to `finalized`, and appends audit evidence.                                                                | Static verified   |
