Implemented Stage 4 bootstrap synthesis work on `feat/stage-4-bootstrap-synthesis`.

Key pieces completed:
- Added migration/schema/contracts/repositories for `project_knowledge_source_links` and `project_knowledge_bootstrap_runs`.
- Added worker bootstrap job with HTML extraction, Gmail alias-history digesting, synthesis orchestration, run status updates, budget warning, and safety rails.
- Wired Settings Knowledge UI for source CRUD, generate button, confirmation gate, recent run polling/status.
- Added focused tests for HTML fetcher, Gmail digest masking, synthesis parsing, run safety rails, repository round trips, and Settings source rendering.
- Updated `docs/03-reference/reference-env.md` for worker-side bootstrap usage and `@mozilla/readability`/`jsdom`.

Validation:
- `pnpm typecheck` passed.
- `pnpm lint` passed.
- `pnpm test:unit` is blocked by the macOS Rollup native optional dependency/signing issue before tests execute: `@rollup/rollup-darwin-arm64 ... not valid for use in process: mapping process and mapped file ... have different Team IDs`. I tried ad-hoc re-signing the native module, but Node still rejects it.