Implemented and pushed on `feat/widen-inbox-projection-rule` in commit `42618558` with the requested message: `feat(inbox): widen projection rule to all non-excluded canonical events`.

The change widens projection membership to every non-excluded canonical event, updates projection rebuild/apply semantics so `lastInboundAt`, `lastOutboundAt`, `lastActivityAt`, bucket behavior, and `lastEventType` follow the Tier 2 rules, and widens the shared projection contract/UI typing to the full canonical event enum. No DB migration was added. I also updated the rebuild callers and flipped/expanded the domain tests for campaign-only, auto-only, lifecycle-only, and mixed histories.

Validation passed with `pnpm typecheck` and `pnpm lint`.

PR: https://github.com/nico-kneler-as/as-comms-platform/pull/310

Local-only files still present and not committed: `.codex-exec.log`, `.brief.md`.